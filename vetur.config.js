module.exports = {
    projects: [
        {
            root: './frontend',
            package: './package.json', // relative to root property
            tsconfig: './jsconfig.json', // relative to root property
        }
    ]
}